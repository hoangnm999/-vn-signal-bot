# ══════════════════════════════════════════════════════
# VN Signal Bot — requirements.txt
# ══════════════════════════════════════════════════════

# Telegram bot
python-telegram-bot==22.7

# Data & computation
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
requests>=2.31.0

# Database
pg8000>=1.30.0

# ML Strategy engine (MLStrategyEngine — RandomForest walk-forward)
scikit-learn>=1.3.0
joblib>=1.3.0

# VN Stock data
vnstock==3.5.1

# ── Vibe-Trading engines ───────────────────────────────
arch>=6.0.0            # VolatilityEngine: GARCH(1,1)
czsc>=0.9.68           # ChanlunEngine: FX→BI→ZS→买卖点 (full mode)
smartmoneyconcepts>=0.0.20  # SMCEngine: ChoCH+BOS+FVG (full mode, fallback nếu thiếu)
pyharmonics>=1.4.0     # HarmonicEngine: XABCD pyharmonics (full mode, fallback nếu thiếu)

# Financial data (FundamentalFilterEngine fallback)
yfinance>=0.2.0        # Fallback cuối cho fundamental data (.VN suffix)
